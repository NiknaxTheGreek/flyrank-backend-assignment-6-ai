# Verification evidence

Initial evidence is generated by the commands documented in the README:

- API behavior tests: `backend/tests/test_api.py`
- Labelled evaluation: `backend/eval_cases.json` and generated `backend/eval_results.json`
- Frontend type check: `pnpm --filter @workspace/flyrank-backend-assignment-6 run typecheck`
- Python dependency audit: `uv pip check`

The observed build output is recorded below so the verification trail stays in the workspace.

## Initial build evidence — 2026-08-22

| Check | Observed result |
| --- | --- |
| `PYTHONPATH=artifacts/flyrank-backend-assignment-6 uv run pytest artifacts/flyrank-backend-assignment-6/backend/tests -q` | **14 passed** |
| `PYTHONPATH=artifacts/flyrank-backend-assignment-6 uv run python -m backend.eval_runner` | 12 cases; category accuracy **0.9167** (11/12), urgency accuracy **1.0000** (12/12), joint accuracy **0.9167** (11/12) |
| `uv pip check` | All 27 installed packages compatible |
| `pnpm --filter @workspace/api-spec run codegen` | Completed and library typecheck passed |
| `pnpm run typecheck` | Completed for libraries, API server, web console, mockup sandbox, and scripts |
| `PORT=25846 BASE_PATH=/ pnpm --filter @workspace/flyrank-backend-assignment-6 run build` | Vite production build completed |
| Routed FastAPI probe | `GET /api/llm/healthz` returned `{"status":"ok"}`; `POST /api/llm/classify` returned a validated `bug` result |
| Browser preview | Console loaded with the FastAPI health indicator online; screenshot saved as `screenshots/flyrank-console.jpg` |

## Live provider checkpoint

**Status: passed — genuine managed-provider request.**

This is distinct from the deterministic stub evaluation and the mocked adapter test. Replit AI Integrations provisioned an OpenAI-compatible managed proxy without placing a credential in source. A temporary FastAPI process was run with `LLM_MODE=openai`, then received a real `POST /llm/classify` request.

| Field | Observed value |
| --- | --- |
| Date | 2026-08-22 |
| Provider | `replit-ai-integrations-openai` |
| Model | `gpt-4.1-mini` |
| Endpoint | `POST /llm/classify` |
| HTTP outcome | `200 OK` |
| Input | “My subscription was charged twice and I need an urgent refund.” |
| Validated classification | `billing`, `high`, confidence `0.95` |
| Validated reason | “Customer reports being charged twice and requests an urgent refund.” |
| End-to-end HTTP latency | `1951.41 ms` |
| API metadata duration | `1941.13 ms` |
| Cache hit | `false` |
| Retries | `0` |
| Input tokens | `130` |
| Output tokens | `31` |

No provider credential, request authorization header, raw provider response, or other secret material is recorded here.